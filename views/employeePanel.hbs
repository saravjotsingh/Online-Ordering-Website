<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
      <meta http-equiv="refresh" content="5" />
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.7/css/bootstrap.min.css">
  </head>
  <body>
    <div class="container">
      <h1>Orders</h1>
      {{#each docs}}

      <div class="row" style="border:1px solid gray;padding:10px">
        <p>Order No:{{id}}</p>
        <div class="col-sm-4">
          {{#each items}}
            <p>{{this}}</p>
          {{/each}}
      </div>
      <div class="col-sm-4">
            {{#each quantity}}
              <p>{{this}}</p>
            {{/each}}
        </div>
        <div class="col-sm-4">
          <button type="button" class="btn btn-success" name="button" value="{{id}}">Complete</button>
        </div>
      </div>
      {{/each}}
    </div>
  </body>
</html>
